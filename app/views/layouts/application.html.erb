<!DOCTYPE html>
<html>
<head>
  <title>BooksReference</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <%= stylesheet_link_tag 'active_admin', media: 'all' if controller.class < ActiveAdmin::BaseController %>
  <%= javascript_include_tag 'active_admin' if controller.class < ActiveAdmin::BaseController %>
</head>

<body class="font-sans bg-gray-100">
  <div class="navbar bg-gray-800 text-white p-4">
    <a class="hover:bg-gray-700 px-3 py-2 rounded" href="<%= root_path %>">Home</a>
    <a class="hover:bg-gray-700 px-3 py-2 rounded" href="<%= agents_path %>">Agents</a>
    <a class="hover:bg-gray-700 px-3 py-2 rounded" href="<%= resources_path %>">Resources</a>
    <a class="hover:bg-gray-700 px-3 py-2 rounded" href="<%= subjects_path %>">Subjects</a>
    <a class="hover:bg-gray-700 px-3 py-2 rounded" href="<%= about_path %>">About</a>
    <a class="hover:bg-gray-700 px-3 py-2 rounded" href="<%= admin_root_path %>">Admin</a>
    <div class="search-container float-right">
      <%= form_with(url: search_path, method: :get, local: true, class: "flex items-center") do %>
        <input type="text" placeholder="Search..." name="q" class="p-2 rounded border border-gray-300">
        <select name="filter" class="ml-2 p-2 rounded border border-gray-300">
          <option value="books">Books</option>
          <option value="agents">Agents</option>
          <option value="resources">Resources</option>
          <option value="subjects">Subjects</option>
        </select>
        <button type="submit" class="ml-2 bg-gray-700 text-white px-3 py-2 rounded hover:bg-gray-600">Search</button>
      <% end %>
    </div>
  </div>

  <div class="content max-w-6xl mx-auto p-4">
    <%= yield %>
  </div>

  <!-- Footer Placeholder -->
  <footer class="bg-gray-800 text-white text-center p-4 mt-8">
    <!-- Add footer content here -->
  </footer>
</body>
</html>
